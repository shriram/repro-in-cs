1:BUILD_BY[name] student4
1:BUILD_ENVIRONMENT[operating system 32 vs 64] ubuntu 32 
1:DEPENDENCIES[list of dependencies with where to get them]

The following packages need to be installed (e.g., `sudo apt-get
install openjdk-7-jdk libssl-dev g++ libfuse-dev` )

    openjdk-7-jdk 
    libssl-dev
    g++
    libfuse-dev

1:END_DEPENDENCIES
1:NOTES[notes on attempted build]
Follow README notes produces results without hitch.

1:END_NOTES
VERIFY:ANALYSIS_BY[name]
VERIFY:STATUS[unknown,needed,not_needed,started,finished] unknown
VERIFY:COMMENT[string]


